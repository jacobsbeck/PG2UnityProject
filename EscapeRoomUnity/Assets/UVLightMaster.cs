using System.Collections;
using System.Collections.Generic;
using UnityEngine;
    
public class UVLightMaster : MonoBehaviour
{

    public bool isUvLight = false;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    void Update()
    {
        if (isUvLight == true)
        {
            //set all scens to uv light mode
        }
        else 
        {
            //set all scenes as normal
        }
        
    }
}
